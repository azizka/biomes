% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomes_occ.R
\name{biomes_occ}
\alias{biomes_occ}
\title{Download and clean GBIF occurrences for a taxon}
\usage{
biomes_occ(
  taxon,
  use_download = FALSE,
  username = NULL,
  pwd = NULL,
  email = NULL,
  save_dir = NULL,
  filter_clean = TRUE,
  filter_sea = FALSE,
  year_min = NULL,
  year_max = NULL,
  country = NULL,
  limit = NULL,
  slim = TRUE
)
}
\arguments{
\item{taxon}{Scientific name(s) to query (species, genus, family, ...).
Accepts a single name or a character vector of names. All matching
keys are bundled into one \code{occ_download()} job; the \code{occ_search()}
path loops over the taxa and splits the user-requested number of
records evenly across them.}

\item{use_download}{Logical. Force the GBIF download workflow even if
the total record count is below 100,000. Default: \code{FALSE}.}

\item{username}{GBIF username (used when the download workflow is
triggered, either via \code{use_download = TRUE} or by the interactive
prompt). If \code{NULL}, the user is asked at the console.}

\item{pwd}{GBIF password (same logic as \code{username}).}

\item{email}{GBIF account email (same logic as \code{username}).}

\item{save_dir}{Directory used for outputs when \code{occ_download()} is
triggered (both the data CSV and a \verb{*_citation.txt}). If \code{NULL}
(default), the user is asked at the console; an empty answer falls
back to the current working directory.}

\item{filter_clean}{Logical. If \code{TRUE}, run basic spatial cleaning on
coordinates with \code{\link[CoordinateCleaner:clean_coordinates]{CoordinateCleaner::clean_coordinates()}}.
Default: \code{TRUE}.}

\item{filter_sea}{Logical. If \code{TRUE}, also flag occurrences in the sea
(\code{"seas"} test). Default: \code{FALSE}.}

\item{year_min}{Optional integer. If supplied, only records with
\code{year >= year_min} are kept on the GBIF side. \code{NULL} (default)
means no year filter.}

\item{year_max}{Optional integer. Same as \code{year_min} but for the
upper bound.}

\item{country}{Optional character vector. One or more ISO 3166-1
alpha-2 country codes (only used in the download workflow).}

\item{limit}{Optional integer. Number of records to download. When
\code{NULL} (default), the user is asked interactively in the
\code{occ_search()} path; the \code{occ_download()} path always returns all
available records.}

\item{slim}{Logical. If \code{TRUE} (default), the result is trimmed to
\verb{family, genus, species, year, countryCode, decimalLongitude, decimalLatitude}. Set to \code{FALSE} to keep all GBIF columns.}
}
\value{
A data frame of GBIF occurrence records, optionally cleaned.
Always contains \code{decimalLongitude} and \code{decimalLatitude} columns
(when records are returned), so the result can be passed directly to
\code{\link[=biomes_classify]{biomes_classify()}}, \code{\link[=biomes_rank]{biomes_rank()}} or \code{\link[=biomes_full]{biomes_full()}}.
}
\description{
Retrieves occurrence records for a given taxon (species, genus, family,
...) from GBIF and, optionally, runs standard coordinate cleaning with
\code{\link[CoordinateCleaner:clean_coordinates]{CoordinateCleaner::clean_coordinates()}}.
}
\details{
By default, \code{biomes_occ()} first asks GBIF how many records exist for
the taxon and then prompts the user (in interactive sessions):
\enumerate{
\item Use \code{rgbif::occ_search()}? (no login required, capped at 100,000
records.) If yes, the user is then asked for the number of records.
If no, the function switches to \code{rgbif::occ_download()}, which
needs a save directory and GBIF credentials and downloads
everything (you get a DOI for citation).
}

The only GBIF predicate applied is \code{hasCoordinate = TRUE}. The result
is slim by default: \code{family}, \code{genus}, \code{species}, \code{year},
\code{countryCode}, \code{decimalLongitude}, \code{decimalLatitude}. Set
\code{slim = FALSE} to keep every GBIF column. With \code{occ_download()} the
downloaded data and the GBIF citation are written to \code{save_dir}.
}
\examples{
\dontrun{
# interactive: prompted for occ_search vs occ_download
occ <- biomes_occ(taxon = "Solemyida")

# force the GBIF download workflow up front (requires credentials)
occ <- biomes_occ(
  taxon        = "Fagus sylvatica",
  use_download = TRUE,
  username     = "xxx",
  pwd          = "xxx",
  email        = "you@example.org",
  save_dir     = "data/GBIF"
)
}

}
