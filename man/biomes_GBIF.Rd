% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomes_GBIF.R
\name{biomes_GBIF}
\alias{biomes_GBIF}
\title{Download and filter GBIF occurrences for multiple taxa}
\usage{
biomes_GBIF(
  taxon,
  filter_clean = FALSE,
  filter_sea = FALSE,
  year_min = NULL,
  year_max = NULL,
  country = NULL,
  limit = NULL
)
}
\arguments{
\item{taxon}{One or more scientific names to query (species, genus etc.). Required.}

\item{filter_clean}{Logical. If \code{TRUE}, run basic spatial cleaning on
coordinates using \code{CoordinateCleaner::clean_coordinates()}. Default: \code{FALSE}.}

\item{filter_sea}{Logical. If \code{TRUE}, also filter occurrences that fall in
the sea (\code{tests} includes "seas"). Default: \code{FALSE}.}

\item{year_min}{Optional integer. Minimum event year for records to keep.}

\item{year_max}{Optional integer. Maximum event year for records to keep.}

\item{country}{Optional character vector. One or more country ISO 3166-1 alpha-2 codes to filter occurrences by country.}

\item{limit}{Optional integer. Record limit passed to \code{rgbif::occ_search()} (per taxon).}
}
\value{
Data frame of GBIF occurrences after optional cleaning.
}
\description{
Retrieve occurrence records for one or multiple taxa from GBIF.
}
\details{
Coordinates can optionally be cleaned using \code{CoordinateCleaner}. No files
are written.
}
\examples{
\dontrun{
dat <- biomes_GBIF(
  taxon = c("Solemyida", "Bufo bufo"),
  year_min = 2000,
  year_max = 2024,
  limit = 5000
)
}

}
